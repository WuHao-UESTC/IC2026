你是一位拥有丰富 ISSCC（国际固态电路会议）发文经验的 Python 数据可视化专家。请帮我编写一段 Python 代码，用于读取集成电路 EDA 仿真导出的 CSV 数据，绘制出符合 IEEE 期刊标准的精美图表，并在图像右上角添加一个清晰的水印。

【数据输入】

数据文件名为：[tia_test.csv]

数据包含[2]列：
第一列代表 [频率 (Frequency)]（单位 [Hz]）；
第二列代表 [增益 (Gain)]（单位 [dB]）。

【图表类型与学术标准】

请使用 matplotlib 和 seaborn 绘制一张 [频率响应对数波特图]。

配色方案： 请使用沉稳且高对比度的学术期刊标准色（例如：深海军蓝 Dark Navy、砖红色 Crimson Red）。

字体规范： 统一使用 Times New Roman 或 Arial。标题字号设为 14，坐标轴标签 12，刻度 10。

线条与坐标轴： 数据线粗细为 1.5 到 2.0，刻度线向内（direction='in'），开启浅灰色虚线网格，保留四周的边框线形成封闭框。

特定设置： [请将 X 轴设置为对数坐标]。

图名：TIA AC Response
输出文件名：与数据文件名保持一致

【特殊要求：关键点标注】

请在图表中自动寻找并清晰标注出两个关键点：

-3dB 带宽点（即低频/最大增益下降 3dB 所对应的频率点）。

单位增益点（Unity Gain，即增益为 0dB 对应的频率点）。

标注的具体要求如下：

在曲线上用醒目的颜色（如砖红色）以散点（scatter）的形式标出这两个点。

添加文本框或指向性标注，在图上绘制出这两个点精确的 X (频率，请转换为合适的 MHz/GHz 单位) 与 Y (增益，dB) 坐标。

核心难点处理：请注意，.csv 的离散数据中极大概率没有恰巧等于目标增益（-3dB 或 0dB）的点。你必须在代码中手动实现针对 X 轴为对数坐标的“对数-线性插值”算法，通过相邻的两组数据点精确计算出穿越点对应的真实频率值，然后再进行标注。绝对不能直接取粗略的最近点。

【特殊要求：添加水印】
请在代码中加入以下水印功能：

水印文本： @CICC1008916

水印位置： 图像的右上角 (top-right corner)，请确保水印不遮挡关键的坐标轴刻度和标签。

水印样式： 使用与坐标轴标签相同的字体。字号设为 10（与刻度字号相同）。

透明度与颜色： 请使用半透明（例如 alpha=0.5）的深灰色或黑色，使其清晰可辨但不会过于突兀。

布局调整： 请确保水印与图表内容之间有适当的间距，使整体布局显得严谨专业。

【代码输出要求】

代码必须包含详细的中文注释。

图表最终需要使用 plt.savefig() 导出，格式为分辨率不低于 600 dpi 的 .png 格式，背景设置为白色（透明度为不透明），并使用 bbox_inches='tight' 去除多余白边。

请直接给出完整的、可独立运行的 Python 脚本。